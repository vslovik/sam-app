# Lazy Flyers

AWS SAM App example

# Requirements
 * AWS account created
 * AWS SAM CLI installed
 * Nodejs installed
 * Docker installed (to run and test application locally)

# To build

$ sam build

# To run locally

$ sam local start-api

# To deploy on AWS

$ sam deploy --guided